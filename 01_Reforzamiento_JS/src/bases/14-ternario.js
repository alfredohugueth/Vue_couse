

let firstName;

let lastName = 'Herrera';

console.log(`${firstName || 'No fistName'} ${ lastName || 'No lastName'}`);


const isActive = false

const msg = isActive ? 'activo' : 'Inactivo'

console.log(msg);
